os: linux
sudo: required
language: "minimal"
services:
  - docker
  
jobs:
  include:
    - name: "tests"
      script:
        - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/tests.sh darthbarada/bootstrap-cpp
    - name: "coverage"
      script:
        - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/coverage.sh darthbarada/bootstrap-cpp
    - name: "quality"
      script:
        - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/duplication.sh darthbarada/bootstrap-cpp
    - name: "memory leaks"
      script:
        - docker run -t -v $(pwd):/opt/builder/ --entrypoint ./scripts/valgrind.sh darthbarada/bootstrap-cpp
notifications:
  email: false
